principal 
{
  ent n, curr, ultim, cuenta, primero;
  curr = 2;
  ultim = 0;
  cuenta = 0;
  primero = 2 - 1;
  ent funcionA(ent ee, ent rer){
 
    real funcionDentro(real aa){
      devolver aa;
    }
    
    devolver 32;
  }

  curr =  funcionA(2, 3) - 2;
  curr = curr +42;
  salida(curr);
  
  real funcionR(ent e2){
    real r1 = 4.6 +3.0;
    devolver r1;
  }
  real r2 = funcionR(3);
  salida(r2);

  devolver 0;
}
