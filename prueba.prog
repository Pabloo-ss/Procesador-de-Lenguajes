principal {
  
  ent n, curr;
    

    salida("i");
    entrada(n);
    salida(" ", n, " == ");
    curr = 2;

    mientras ( curr < n ) {

      ent d;
      d = n / curr;
    }
    salida("\n");
  devolver 0;
}
